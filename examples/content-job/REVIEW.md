# Example component review

Uses the existing `step_cards` and `link_cards` components. No new component
code is required for this topic. Text is grounded in the sample transcript.
Review spacing at phone and desktop widths, keyboard activation of each card,
and opening/closing the step disclosures at `/component-library` locally.

Video filenames are placeholders. This fixture demonstrates the component stage,
not media upload or publication.
