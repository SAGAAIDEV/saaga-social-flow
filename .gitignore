# Rust build artifacts
/target/
**/*.rs.bk
Cargo.lock

# IDE and editor
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Environment
.env
.env.local
.env.*.local

# OS files
*.log
Thumbs.db

# Development
debug/
*.deb

# SQLite database
*.db
*.db-shm
*.db-wal

# Process id files
*.pid

# SOPS: the encrypted *.sops.env files ARE committed. These are the decrypted
# forms — sops' own temp file, and the usual names for a hand-decrypted copy.
*.sops.env.decrypted
*.decrypted.env
dev.env
prd.env

# Node dependencies of renderer/ — installed by scripts/setup.sh (npm ci)
node_modules/

# Terraform: providers and plans are local; .terraform.lock.hcl is committed.
.terraform/
tfplan

# Python bytecode from checking infra/gpu-render/worker locally.
__pycache__/
